/**
 * Борг за розміром сенсорних цілей — дані для `touch-targets.spec.ts`.
 *
 * Окремим модулем, як `a11y-baseline.ts`: борг мусить читатися без прогону
 * гейта, і правити його має бути видно в diff окремим рядком.
 *
 * ## Чому перелік підписів, а не число
 *
 * Число ховає підміну: чотирнадцять порушень, з яких сім старих і сім нових,
 * дають те саме «14». Перелік підписів валить прогін на ПЕРШІЙ новій цілі,
 * навіть коли загальна кількість не зросла. Той самий висновок, що й у
 * `a11y-baseline.ts` про вузли проти правил.
 */

/**
 * Нормативний мінімум SC 2.5.8 (рівень AA), у CSS px. Діє на будь-якому
 * вказівнику — мишею теж.
 */
export const TOUCH_MIN = 24;

/**
 * Мінімум канону на ДОТИКУ (UI-ELEMENTS-v8 § 1), у CSS px.
 *
 * Доти цей поріг у гейті не міряв ніхто, і причина була записана прямо:
 * «Playwright ходить мишею, тобто `coarse`-гілка в замір не потрапляє». Це
 * виявилося неправдою — контекст із `hasTouch: true` дає
 * `matchMedia('(pointer: coarse)').matches === true`, — і рівно за цією
 * неправдою жив справжній дефект: єдине правило `@media (pointer: coarse)` у
 * підвалі стояло ВИЩЕ за базове тієї ж специфічності й не діяло ніколи.
 *
 * Тобто сліпою зоною була не техніка, а твердження про неї.
 */
export const TOUCH_MIN_COARSE = 44;

/**
 * Відомі цілі нижче межі `TOUCH_MIN`, за маршрутами. Підпис — `data-testid`,
 * якщо він є, інакше текст, далі `aria-label`, і лише потім тег.
 *
 * Єдиний запис — крапки каруселі в чернетці `/test`: заміряно 35×6 на телефоні
 * і 40×6 на десктопі, перша ширша (70×6 і 80×6 — активна). Не заокруглення:
 * висота цілі там дорівнює висоті самої крапки, тобто вчетверо менша за
 * мінімум SC 2.5.8.
 *
 * Чому борг, а не правка. Сторінки немає ні в індексі, ні в меню, а карусель —
 * чернетка, яку ще нікуди не вбудовано. Файл `/test/+page.svelte` при цьому
 * стоїть у `SIZE_DEBT` (1280 SLOC проти межі 400), тобто рости йому не можна:
 * дописати сюди стилі означало б розміняти зафіксований борг розміру на
 * причісування чернетки. Коли карусель поїде в бойову сторінку, цей рядок
 * впаде разом із нею — і саме тоді правка буде доречною.
 *
 * Підпис приходить із хардкоджених `aria-label` (`Слайд N`) — тих самих трьох,
 * що названі в `PROJECT-CONTEXT.md` як відоме відхилення `/test` від I18N-v8.
 * Локалізують їх — зміниться й цей перелік; гейт про це скаже.
 *
 * Відсутність маршруту в мапі — теж твердження: «тут порушень немає».
 */
export const TOUCH_DEBT: Record<string, readonly string[]> = {
	'/test': ['Слайд 1', 'Слайд 2', 'Слайд 3', 'Слайд 4', 'Слайд 5', 'Слайд 6']
};

/**
 * Борг за порогом дотику (44×44). Окрема мапа, а не спільна з `TOUCH_DEBT`:
 * пороги різні, тож і переліки різні, а одна мапа на два пороги означала б, що
 * ціль 30×30 мовчки прощається на обох.
 *
 * Ті самі крапки каруселі чернетки й з тієї самої причини — плюс сім посилань
 * «Читати далі →» на 154.91×43.51 px. Останні варті окремого слова: у звіті вони
 * друкуються як 155×44, тобто ВИГЛЯДАЮТЬ такими, що межу проходять. Не проходять
 * на пів пікселя, і саме тому порівняння в гейті йде по НЕзаокругленій висоті:
 * заокруглення перед порівнянням пропускало б цілі, які на пристрої на пів
 * пікселя менші за норму.
 */
export const TOUCH_DEBT_COARSE: Record<string, readonly string[]> = {
	'/test': [
		'Слайд 1',
		'Слайд 2',
		'Слайд 3',
		'Слайд 4',
		'Слайд 5',
		'Слайд 6',
		'Читати далі →'
	]
};

/**
 * Найбільший перетин двох різних цілей, який ще не є крадіжкою кліку, у CSS px
 * (ACCESSIBILITY-v9 § 10.3.1, `A11Y-TOUCH-OVERLAP`).
 *
 * Число з канону. Менші перетини бувають від заокруглення підпікселів і
 * рамок; більший означає, що частина однієї цілі лежить під іншою.
 */
export const TOUCH_OVERLAP_MAX = 4;

/**
 * Пари, яким перетинатися дозволено, — і ЛИШЕ тому, що перекриття там є
 * навмисним нашаруванням, а не наслідком розкладки.
 *
 * Єдиний запис — клавіші піаніно. На клавіатурі чорна клавіша лежить ПОВЕРХ
 * білої за побудовою інструмента, і саме цього чекає той, хто натискає:
 * заміряно 2026-09-10 у портреті 390×844 (піаніно там повернуте на 90°) —
 * 14 пар по 129×23 px, у кожній чорна клавіша має `z-index: 3` проти `2` у
 * білої, і `elementFromPoint` у центрі перетину віддає саме чорну.
 *
 * Виняток НЕ звільняє пару від перевірки: гейт окремо доводить, що
 * нашарування однозначне (різний `z-index` І влучання у верхній елемент).
 * Інакше запис у цьому переліку означав би «ми на це подивилися колись», а не
 * «конфлікт вирішено» — і першу ж помилку в `z-index` він би сховав.
 *
 * Клас, а не 14 літеральних пар: число клавіш задає розкладка, і перелік пар
 * довелося б правити щоразу, коли додається октава.
 */
export const OVERLAP_ALLOWED_PAIR = /^piano-key-\d+-btn$/;
